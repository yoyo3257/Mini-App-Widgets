import 'package:flutter/material.dart';


class InputWidgetsAndRichText extends StatelessWidget {
  const InputWidgetsAndRichText({super.key});
  @override
  Widget build(BuildContext context) {
    return const Text('Input Widgets and RichText Content', style: TextStyle(fontSize: 20, color: Colors.indigo));
  }
}