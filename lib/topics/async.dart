import 'package:flutter/material.dart';
class AsyncWidgets extends StatelessWidget {
  const AsyncWidgets({super.key});
  @override
  Widget build(BuildContext context) {
    return const Text('Async Widgets Content', style: TextStyle(fontSize: 20, color: Colors.indigo));
  }
}