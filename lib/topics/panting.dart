import 'package:flutter/material.dart';


class PaintingAndEffectWidgets extends StatelessWidget {
  const PaintingAndEffectWidgets({super.key});
  @override
  Widget build(BuildContext context) {
    return const Text('Painting and Effect Widgets Content', style: TextStyle(fontSize: 20, color: Colors.indigo));
  }
}