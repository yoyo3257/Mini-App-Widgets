import 'package:flutter/material.dart';

class ScrollingWidgets extends StatelessWidget {
  const ScrollingWidgets({super.key});
  @override
  Widget build(BuildContext context) {
    return const Text('Scrolling Widgets Content', style: TextStyle(fontSize: 20, color: Colors.indigo));
  }
}
