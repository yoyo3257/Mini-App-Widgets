import 'package:flutter/material.dart';

class InteractionModelWidgets extends StatelessWidget {
  const InteractionModelWidgets({super.key});
  @override
  Widget build(BuildContext context) {
    return const Text('Interaction Model Widgets Content', style: TextStyle(fontSize: 20, color: Colors.indigo));
  }
}